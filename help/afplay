# afplay（macOS 內建音訊播放器）

── 基本 ───────────────────────────────────────────────────────

  afplay file.mp3    播放音訊檔，播完自動結束
  支援格式            mp3 / m4a / wav / aiff / caf（AudioToolbox）
  位置                /usr/bin/afplay，系統內建免安裝

── 參數 ───────────────────────────────────────────────────────

  -v 0.4         音量（1.0 = 原音量，相對於系統音量）
  -t 3           只播前 3 秒
  -r 2           播放速率（2 = 兩倍速）
  -q 1           變速時使用高品質重取樣

── 實際用途 ───────────────────────────────────────────────────

  Claude Code hook 通知音效（見 claude/settings.json）
  afplay -v 0.4 ~/.claude/sounds/notification.mp3

  Linux 對應指令：paplay / aplay
